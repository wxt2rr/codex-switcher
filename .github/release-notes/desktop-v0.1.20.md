## Desktop v0.1.20

- 账号页环境筛选新增默认环境星标，可直接将任意环境设为账号页默认筛选项。
- 默认环境状态使用实心星标展示，其他环境使用可点击的空心星标，减少下拉菜单文字干扰。
- 修复设置默认环境后切换页面再返回账号页时，筛选项没有恢复为所设环境的问题。
- 默认环境被删除时自动回退到 `default` 或第一个可用环境。

## 安装包

- macOS：Apple Silicon DMG / ZIP
- Windows：x64 NSIS 安装程序

> 当前安装包未进行 Apple 公证或 Windows 代码签名，首次启动时系统可能显示安全提示。
